from setuptools import setup
setup (name = 'AsciiArt',
    version = '0.1.01',
    description = 'Ascii "art" from images',
    author = 'clara, marie',
    packages = ['asciiArt'],
    install_requires = ['pillow', 'requests'])
